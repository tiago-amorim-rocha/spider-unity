name: Build WebGL

on:
  workflow_dispatch:
    inputs:
      buildName:
        description: "Name of the build (folder/artifact suffix)"
        required: false
        default: "webgl-build"

  push:
    branches:
      - main
    paths:
      - "Assets/**"
      - "ProjectSettings/**"
      - "Packages/**"

jobs:
  build-webgl:
    runs-on: ubuntu-latest

    env:
      UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
      UNITY_VERSION: "6000.2.8f1"
      BUILD_NAME: ${{ github.event.inputs.buildName || 'webgl-build' }}

    steps:
      - name: Free up disk space
        run: |
          echo "Disk space before cleanup:"
          df -h

          # Remove unnecessary pre-installed software
          sudo rm -rf /usr/share/dotnet
          sudo rm -rf /usr/local/lib/android
          sudo rm -rf /opt/ghc
          sudo rm -rf /opt/hostedtoolcache/CodeQL
          sudo rm -rf "$AGENT_TOOLSDIRECTORY"

          # Clean apt cache
          sudo apt-get clean

          # Remove Docker images
          docker rmi $(docker images -q) -f || true

          echo "Disk space after cleanup:"
          df -h

      - name: Checkout
        uses: actions/checkout@v4

      - name: Cache Unity Library
        uses: actions/cache@v4
        with:
          path: Library
          key: Library-${{ runner.os }}-${{ env.UNITY_VERSION }}-${{ hashFiles('Assets/**', 'Packages/**', 'ProjectSettings/**') }}
          restore-keys: |
            Library-${{ runner.os }}-${{ env.UNITY_VERSION }}-

      - name: Build WebGL
        uses: game-ci/unity-builder@v4
        env:
          UNITY_LICENSE: ${{ secrets.UNITY_LICENSE }}
        with:
          targetPlatform: WebGL
          unityVersion: ${{ env.UNITY_VERSION }}
          allowDirtyBuild: true
          buildName: ${{ env.BUILD_NAME }}
          buildsPath: build

      - name: Upload WebGL build artifact
        uses: actions/upload-artifact@v4
        with:
          name: ${{ env.BUILD_NAME }}
          path: build/WebGL/${{ env.BUILD_NAME }}
